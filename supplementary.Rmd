---
title: "Supplementary"
author: "Ruoyu Wu"
date: "2023-07-4"
output:
  html_document: default
---

```{r setup, include=FALSE, message=FALSE}
knitr::opts_chunk$set(echo = FALSE, cache=TRUE,  warning = FALSE, message = FALSE)
source("code/0_utils.R")
df_smooth <- read_rds(str_c(.path$dat_process, "2023-04-25/fill_smooth_offset.rds"))
df_metrics <- read_rds(str_c(.path$dat_process, "2023-04-25/metrics_daymet.rds"))
```

# Table S1
```{r trend-flags-tab}
source("code/21_spp_trend_flag_tab.R")
df_trend_flag %>%
  knitr::kable(caption = "Temporal trends of 7 fungal spore phenology metrics. Displayed is the slope of spore metrics with ln-transformation for concentrations, change over 2003-2022 in % for concentrations (back-transformed) and days for start date and season length metrics, and the p-value of the mixed effexts model.", col.names = NULL)
```

